import express from "express";

const router = express.Router();

// Reserved for future webhooks (e.g. Stripe). User provisioning uses Supabase JWT + Neon (see middleware/auth.js).

export default router;
